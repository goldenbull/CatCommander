<Project Sdk="Microsoft.NET.Sdk">

    <!--
      Vendored, not fetched from NuGet - see NOTICE.md for why and where this came from.
      Kept as close to upstream as possible so future bugfixes here stay easy to diff
      against a fresh checkout of the same commit.
    -->
    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <RootNamespace>Avalonia.Controls</RootNamespace>
        <IsPackable>false</IsPackable>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia" Version="11.3.20" />
        <PackageReference Include="System.Reactive" Version="6.1.0" />
    </ItemGroup>

</Project>
