<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CatCommander.ViewModels"
        xmlns:view="using:CatCommander.View"
        x:Class="CatCommander.View.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="CatCommander"
        Icon="/Assets/cat.png"
        Width="1024"
        Height="640"
        WindowState="Maximized">

    <!--
      One NativeMenu model drives both platforms: on macOS it becomes the system menu bar (via
      NativeMenu.Menu below), and the NativeMenuBar control placed in the tree further down
      renders it as an in-window bar on Windows/Linux (and is a no-op on macOS) - no need to
      define the menu twice.
    -->
    <NativeMenu.Menu>
        <NativeMenu>
            <NativeMenuItem Header="File">
                <NativeMenu>
                    <NativeMenuItem Header="Find..." Gesture="Alt+F7" Command="{Binding OpenFindCommand}" Icon="/Assets/Icons/find.png" />
                    <NativeMenuItem Header="Batch Rename..." Command="{Binding OpenBatchRenameCommand}" Icon="/Assets/Icons/batch_rename.png" />
                </NativeMenu>
            </NativeMenuItem>
            <NativeMenuItem Header="Edit">
                <NativeMenu>
                    <NativeMenuItem Header="Copy" Gesture="F5" Command="{Binding CopyCommand}" Icon="/Assets/Icons/copy.png" />
                    <NativeMenuItem Header="Move" Gesture="F6" Command="{Binding MoveCommand}" Icon="/Assets/Icons/move.png" />
                    <NativeMenuItem Header="Rename" Gesture="F2" Command="{Binding RenameCommand}" Icon="/Assets/Icons/rename.png" />
                    <NativeMenuItem Header="Delete" Gesture="F8" Command="{Binding DeleteCommand}" Icon="/Assets/Icons/delete.png" />
                </NativeMenu>
            </NativeMenuItem>
            <NativeMenuItem Header="Settings">
                <NativeMenu>
                    <NativeMenuItem Header="Restore Default Shortcuts" Command="{Binding RestoreDefaultShortcutsCommand}" />
                </NativeMenu>
            </NativeMenuItem>
            <OnPlatform>
                <OnPlatform.macOS>
                    <NativeMenuItem Header="About">
                        <NativeMenu>
                            <NativeMenuItem Header="About CatCommander" />
                        </NativeMenu>
                    </NativeMenuItem>
                </OnPlatform.macOS>
            </OnPlatform>
        </NativeMenu>
    </NativeMenu.Menu>

    <DockPanel LastChildFill="True">
        <NativeMenuBar DockPanel.Dock="Top" />

        <!-- Toolbar -->
        <Border DockPanel.Dock="Top" BorderBrush="{DynamicResource TcBorderBrush}" BorderThickness="0,0,0,1" Padding="2">
            <StackPanel Orientation="Horizontal" Spacing="2">
                <StackPanel.Styles>
                    <Style Selector="Button > StackPanel">
                        <Setter Property="Orientation" Value="Horizontal" />
                        <Setter Property="Spacing" Value="4" />
                    </Style>
                    <Style Selector="Button Image">
                        <Setter Property="Width" Value="16" />
                        <Setter Property="Height" Value="16" />
                    </Style>
                </StackPanel.Styles>

                <Button Command="{Binding CopyCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/copy.png" />
                        <TextBlock Text="Copy (F5)" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding MoveCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/move.png" />
                        <TextBlock Text="Move (F6)" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding RenameCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/rename.png" />
                        <TextBlock Text="Rename (F2)" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding DeleteCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/delete.png" />
                        <TextBlock Text="Delete (F8)" />
                    </StackPanel>
                </Button>

                <Separator Width="1" />

                <Button Command="{Binding OpenFindCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/find.png" />
                        <TextBlock Text="Find" />
                    </StackPanel>
                </Button>
                <Button Command="{Binding OpenBatchRenameCommand}">
                    <StackPanel>
                        <Image Source="/Assets/Icons/batch_rename.png" />
                        <TextBlock Text="Batch Rename" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Main panes - fixed 1:1 split, no draggable splitter. -->
        <Grid Margin="4" ColumnDefinitions="*,4,*">
            <view:MainPanel Grid.Column="0" DataContext="{Binding LeftPanel}" />
            <!-- Just a spacing gap now, not a divider line - each TreeDataGrid already draws its
                 own 1px border (see ClassicTheme.axaml), so a second gray line here was redundant. -->
            <Border Grid.Column="1" Background="Transparent" />
            <view:MainPanel Grid.Column="2" DataContext="{Binding RightPanel}" />
        </Grid>
    </DockPanel>
</Window>
